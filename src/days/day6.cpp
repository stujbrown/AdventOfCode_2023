
void aoc::day6()
{
    std::ifstream file("inputs/day6.txt");
    std::string token;
    int line_idx = 0;
    std::vector<int> durations, times;
    while (file >> line)
    {
        if (std::isalpha(line))
    }
    
}
